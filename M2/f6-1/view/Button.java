package view;
import controller.Controller;

public class Button {
    private Controller ctrl;
    
    public void depositButton() {
        // code to input amount
        ctrl.deposit(amount);
    }
}
