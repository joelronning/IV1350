package view;

import model.userDTO.UserDTO;

class View {
    public void registerUser(String args[]) {
        //UserDTO user = new UserDTO(args[]);
        controller.Controller.registerUser(user);
    }
}
